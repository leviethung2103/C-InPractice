#include<iostream>
#include<stdio.h>
#include<vector>
#include<string.h>
using namespace std;

int main () 
{
    int arr[] = { 1, 5, 8, 9, 6, 7, 3, 4, 2, 0 };
    double length = sizeof(arr) / sizeof(int);
    cout << length << endl; 
}